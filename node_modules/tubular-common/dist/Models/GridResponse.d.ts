export declare class GridResponse {
    aggregationPayload: Record<string, number>;
    counter: number;
    currentPage: number;
    filteredRecordCount: number;
    payload: any;
    totalPages: number;
    totalRecordCount: number;
    constructor(counter?: number);
}
