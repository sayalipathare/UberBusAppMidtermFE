import * as React from 'react';
export interface TitleProps {
    prefix?: string;
    suffix?: string;
    titleClass?: string;
    value: string;
}
export declare const Title: React.FunctionComponent<TitleProps>;
export declare function AppTitle(prefix?: string, suffix?: string): ({ value, titleClass }: TitleProps) => JSX.Element;
