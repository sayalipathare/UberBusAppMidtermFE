import { SimpleObservable } from 'uno-js';
import { MessageType } from './GlobalSnackbar';
export interface Snackbar {
    message: {
        messageText: string;
        messageType: MessageType;
    };
    milliSeconds?: number;
    isMobile: boolean;
}
declare class SnackbarService extends SimpleObservable {
    private snackbar;
    getSnackbar(): Snackbar;
    showSnackbar(message: string, type?: MessageType, milliSeconds?: number): Promise<{}>;
}
export declare const snackbarService: SnackbarService;
export {};
